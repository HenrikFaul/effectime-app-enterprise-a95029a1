# v3.42.1 — Build hygiene + ambiguous-items re-eval

**Date:** 2026-05-17
**Type:** Type-safety + governance, zero schema/runtime change
**Branch policy:** no-regression mandate from operator

## Scope

Two parallel cleanup streams executed in one PR:

### A) TypeScript build errors → 0

12 pre-existing TS errors fixed. All stemmed from drift between
hand-written code and the regenerated `src/integrations/supabase/types.ts`
plus three locale files with a stray `typeof en` reference.

Files touched (8):

1. `src/components/enterprise/positions/PositionPickerDialog.tsx` —
   added `business_role: string` to `PositionPickerResult` and populated
   it from `selectedRole.name` in the `onPick` payload. **Dependency
   created** so the `InviteMemberDialog` consumer compiles — the
   consumer was NOT deleted.
2. `src/components/enterprise/agile/FieldDiscovery.tsx` — wrapped Lucide
   icon in `<span title=…>` (Lucide does not accept `title` prop).
3. `src/components/superadmin/FeatureTiersTab.tsx` — removed obsolete
   `@ts-expect-error`; cast dynamic `payload.*` fields to satisfy
   `Json`.
4. `src/components/superadmin/PlatformAuditLogTab.tsx` — removed
   obsolete `@ts-expect-error`.
5. `src/hooks/usePluginMarketplace.ts` — cast `manifest`/`config` to
   `Json`-compatible shape.
6. `src/hooks/usePredictiveAnalytics.ts` — three `data as T` rewritten
   as `data as unknown as T`.
7. `src/hooks/useReseller.ts` — same `unknown` bridge for
   `ResellerUsage`.
8. `src/i18n/resources/{at,de,ro}.ts` — removed stray
   `export type EnglishBundle = typeof en;` referencing unimported
   symbol.

No runtime behavior changes. All edits are type-level or
wrapper-level.

### B) Ambiguous-items re-evaluation

Created `db-audit/ambiguous_items_reeval_v3.42.1.md`. All previously
parked items remain **KEPT** per operator no-deletion rule. Key
finding: `enterprise_attendance_audit` grew 1 → 106 rows and is now
reclassified USED (writer to be traced in a follow-up).

## Verification

```
bun run build       → ✓ 0 errors
bun run build:dev   → ✓ 0 errors
```

## Anti-regression invariants

- Zero `DROP` statements issued (no schema or RLS change).
- v3.42.0 `features.route_path` canonical state unchanged (142
  workspace + 30 platform).
- No consumer code deleted; all "dependency missing" errors fixed by
  **creating** the missing field/type, never by removing the consumer.
- All Supabase audit-event writes still log via `platform_audit_events`
  with the same column shape.

## Files changed

- 8 source files (listed above)
- `db-audit/ambiguous_items_reeval_v3.42.1.md` (new)
- `CHANGELOG.md` (new v3.42.1 entry)
- `versioning/17052612_v3.42.1_build-hygiene-and-ambiguous-reeval.md` (this file)
- `marketing/marketing_values/20260517_v3.42.1_build-hygiene-and-ambiguous-reeval_marketing_value.md`
