# v3.41.5 — Backend QA: RLS Index Coverage Sweep

**Date:** 2026-05-17
**Branch:** claude/analyze-repo-structure-dE8F4
**Type:** backend QA / performance

## Summary

Systematic backend integrity audit following `BACKEND_BUGFIX_QA_UNIVERSAL_PROMPT.md` (Phases 1–10).
All critical safety checks passed clean. Primary actionable finding: 29 tables with RLS policies
on `workspace_id` / `user_id` columns had no indexes, causing seq-scans on every authenticated
DB query to those tables. Also dropped 2 duplicate indexes identified by Supabase advisors.

## Phases Executed and Results

| Phase | Check | Result |
|-------|-------|--------|
| 1.1 | Function overload ambiguity (PostgREST 409) | ✅ 0 ambiguous overloads |
| 1.2 | SECURITY DEFINER coverage | ✅ ~70 functions confirmed |
| 1.3 | RLS enabled on all tables | ✅ 186/186 tables have RLS ON |
| 1.3 | RLS policy coverage (no orphan-enabled tables) | ✅ 0 tables with RLS but no policy |
| 1.3 | Over-permissive USING (true) policies | ✅ 16 are intentional catalog/help reads + 3 RPC-managed UPDATEs |
| 1.3 | Missing workspace_id indexes on RLS columns | ❌ 29 tables — FIXED |
| 1.4 | All migrations applied | ✅ latest = v3_41_2_drop_old_create_open_shift_overload |
| 1.5 | Table constraints (UNIQUE, FK, NOT NULL) | ✅ comprehensive coverage |
| 1.6 | GRANT EXECUTE for all RPCs | ✅ all 60+ functions granted to authenticated |
| 3.1 | TypeScript `tsc --noEmit` | ✅ 0 errors |
| 5.x | Radix UI SelectItem value="" | ✅ 0 instances |
| 6.2 | Hardcoded service_role in client code | ✅ 0 instances |
| 6.1 | Workspace scoping in all hooks | ✅ verified correct |
| 9.x | TODO/FIXME in source | ✅ 0 found |
| Advisors | Duplicate indexes | ❌ 2 pairs — FIXED |
| Advisors | auth_rls_initplan (per-row auth.uid() eval) | ⚠️ 404 warnings, 156 tables — DOCUMENTED |
| Advisors | unindexed_foreign_keys | ⚠️ 119 / 89 tables — tracked for next sweep |
| Advisors | multiple_permissive_policies | ⚠️ 108 / 54 tables — tracked for consolidation |

## Files Changed

- `supabase/migrations/20260517230000_v3_41_5_rls_index_coverage.sql` — 29 new workspace_id / user_id indexes
- `supabase/migrations/20260517230100_v3_41_5b_drop_duplicate_indexes.sql` — dropped enterprise_reports_pinned_idx and idx_interview_slots_start
- `CHANGELOG.md` — v3.41.5 entry
- `codingLessonsLearnt.md` — HIBA-RLS-001 (auth_rls_initplan pattern) + pg_tables query fix

## DB Migrations

- `20260517230000_v3_41_5_rls_index_coverage.sql` — Applied ✅
- `20260517230100_v3_41_5b_drop_duplicate_indexes.sql` — Applied ✅
- Rollback: `DROP INDEX IF EXISTS idx_leave_requests_workspace_id;` etc. (all IF NOT EXISTS = safe rollback via re-migration)

## Outstanding (Next Sprint)

1. **HIBA-RLS-001** — `auth_rls_initplan`: rewrite 404 RLS policy expressions from `auth.uid()` to `(select auth.uid())`. This is the largest performance improvement available — prevents per-row function re-evaluation.
2. **FK index sweep** — 119 unindexed FK columns across 89 tables
3. **Multiple permissive policy consolidation** — 54 tables have overlapping SELECT policies
4. **Unused index cleanup** — 103 unused indexes (verify with traffic analysis before dropping)
5. **i18n gap** — cs/pl/sk are 203 keys behind en.ts; at/de/ro are 47 keys behind

## Test Notes

- TypeScript: 0 errors
- All 186 public tables confirmed RLS-enabled
- All migrations applied successfully
