# v3.42.2 — Premium UI/UX Refactor: design-system elevation pass

**Date:** 2026-05-18
**Branch:** claude/analyze-repo-structure-dE8F4
**Type:** Visual / design-system (non-breaking)

## Summary

Systematic visual upgrade across all base UI primitives and core surfaces.
Zero functional regression — only styling changed, no props or behavior.

## Files changed

| File | What changed |
|------|-------------|
| `src/index.css` | Refined HSL tokens, new `--shadow-premium`/`--shadow-subtle` variables, new utility classes, `prefers-reduced-motion` global rule |
| `tailwind.config.ts` | Added `premium`/`subtle` boxShadow keys; shimmer keyframe rewritten to `transform`-based for GPU acceleration |
| `src/components/ui/card.tsx` | `rounded-xl`, `shadow-card`, `border-border/70`, tighter padding and CardTitle size |
| `src/components/ui/button.tsx` | `rounded-lg`, `active:scale-[0.98]`, hover lift (primary), `h-9` default size |
| `src/components/ui/badge.tsx` | Smaller text, `tracking-wide`, new `subtle` variant, `BadgeVariant` type export |
| `src/components/ui/input.tsx` | `h-9`, `rounded-lg`, `transition-colors`, muted placeholder |
| `src/components/ui/select.tsx` | `h-9 rounded-lg` trigger; `rounded-xl shadow-elevated` content |
| `src/components/ui/dialog.tsx` | `rounded-2xl shadow-modal`; overlay `backdrop-blur-sm`; close button `rounded-lg` |
| `src/components/ui/popover.tsx` | `rounded-xl shadow-elevated border-border/70` |
| `src/components/ui/dropdown-menu.tsx` | `rounded-xl shadow-elevated` content; `rounded-lg focus:bg-accent/60` items |
| `src/components/ui/avatar.tsx` | Fallback: `bg-primary/10 text-primary font-medium` |
| `src/components/ui/skeleton.tsx` | Directional shimmer via `::before` sweep; replaces `animate-pulse` |
| `src/components/ui/sidebar.tsx` | Active nav items: `bg-sidebar-primary/10 text-sidebar-primary font-semibold`; `rounded-lg` items |
| `src/components/enterprise/WorkspaceDashboard.tsx` | Header `shadow-subtle`; buttons `h-7`; tab active state primary-colored |
| `src/pages/Landing.tsx` | Feature cards: `shadow-subtle hover:-translate-y-0.5` lift |

## Testing

- TypeScript: 0 errors (`npx tsc --noEmit`)
- Production build: passes (`npm run build`)

## No regressions

All component APIs unchanged. Only Tailwind class strings and CSS custom
property values modified. Existing callers and tests unaffected.
