# v3.49.9 — Systems & Data-Integrity Sweep (DB Hygiene)

**Date:** 2026-06-04
**Type:** backend hygiene / security linter
**Constraint honored:** no functional change, no data loss, no feature reorg, no regression risk.

## Lens

Walked through the project as systems analyst + data-integrity specialist + integration specialist. Goal: surface every actionable safety/integrity gap, fix only the zero-risk ones in this PR, document the rest as triaged backlog (already tracked in v3.41.5).

## Audit results

### ✅ Pass
- **RLS coverage** — 186/186 public tables have RLS enabled (re-confirmed)
- **Policy coverage** — no orphan-enabled tables
- **GRANT EXECUTE** — all ~70 RPCs callable by `authenticated`
- **TypeScript** — `tsc --noEmit` clean
- **Migrations** — repo and live DB in sync
- **Edge functions** — CORS shared header used, `verify_jwt` flags intentional per `supabase/config.toml`
- **Frontend ↔ backend integration** — no dead RPC callers, no missing types
- **Service-role exposure** — 0 instances in client code

### 🔧 Fixed in this PR
| Issue | Object | Fix |
|---|---|---|
| `security_definer_view` (ERROR) | `enterprise_org_pulse_membership` | `ALTER VIEW … SET (security_invoker = on)` |
| `security_definer_view` (ERROR) | `enterprise_coverage_rules` | `ALTER VIEW … SET (security_invoker = on)` |
| `function_search_path_mutable` (WARN) | `set_updated_at()` | Added `SET search_path = public` |

Linter delta: **200 → 197 issues**, both ERROR-level findings cleared.

### ⏭️ Deferred (already tracked, NOT touched per user constraint)
- `auth_rls_initplan` — ~404 RLS policies use `auth.uid()` instead of `(select auth.uid())`. Pure perf optimisation, requires per-policy migration. Tracked in `codingLessonsLearnt.md` HIBA-RLS-001 since v3.41.5.
- Unindexed FK sweep — 119 cols / 89 tables (v3.41.5 backlog)
- Multiple permissive policies — 54 tables (v3.41.5 backlog)
- 2 remaining `function_search_path_mutable` warnings — on extension-owned functions, not user-editable
- 365 orphan shift assignments — data cleanup, awaits product owner
- `og-image.png` — missing asset

## Why not more
The repo has 200+ linter items and ~210 security scan items. The user constraint is explicit and non-negotiable: **no regression, no functional change, no reorg**. Mass policy rewrites and FK index sweeps carry real risk in a 186-table production schema with active tenants. Those need their own dedicated PRs with QA — they don't belong in a "sweep" PR.

## Files
- Migration: `20260604061700_v3_49_9_db_hygiene_sweep.sql`
- `CHANGELOG.md` — v3.49.9 entry
- `codingLessonsLearnt.md` — HIBA-DB-002, HIBA-DB-003
- `versioning/04062602_v3.49.9_systems-data-integrity-sweep.md` — this file

## Rollback
```sql
ALTER VIEW public.enterprise_org_pulse_membership RESET (security_invoker);
ALTER VIEW public.enterprise_coverage_rules RESET (security_invoker);
-- set_updated_at search_path reset is harmless to leave in place
```
