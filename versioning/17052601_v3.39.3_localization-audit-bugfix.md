# v3.39.3 — Code-audit bug-fix batch: localization, compact mode, type safety

**Date:** 2026-05-17  
**Branch:** claude/analyze-repo-structure-dE8F4  
**Type:** Bug fix

## Summary

Post-release code audit on the v3.39.x smart-staffing feature set. Ten issues identified; five addressed in this patch (the rest are design choices or low-priority naming).

## Changes

### `src/components/enterprise/calendar/CoveragePlannerView.tsx`
- Line 273: hardcoded Hungarian toast `${name} beosztva (${iso})` → `t('coverage_planner.member_assigned_toast', { name, date })`
- Lines 512/519: view-toggle button labels "Heti" / "Havi" → `t('coverage_planner.weekly')` / `t('coverage_planner.monthly')`
- Line 777: section heading "Beosztva" → `t('coverage_planner.assigned_label')`
- Line 831: overflow label "Extra (slot felett)" → `t('coverage_planner.overflow_label')`

### `src/components/enterprise/calendar/OpenShiftManager.tsx`
- Compact mode rewritten: badge click now expands a panel showing all existing open broadcasts (each with a cancel ×) plus the new-broadcast form. Previously existing broadcasts were invisible after the badge was clicked.
- `CompactFormFields.t` prop type corrected: `Record<string, string>` → `Record<string, string | number>`

### `src/i18n/resources/*.ts` — all 8 locale files (en, hu, de, at, cs, sk, pl, ro)
Four new keys added to `coverage_planner` namespace after `assigned_label`:
- `weekly` — "Weekly" / "Heti" / "Wöchentlich" / etc.
- `monthly` — "Monthly" / "Havi" / "Monatlich" / etc.
- `overflow_label` — "Extra (over quota)" / "Extra (slot felett)" / etc.
- `member_assigned_toast` — "{{name}} assigned ({{date}})" / "{{name}} beosztva ({{date}})" / etc.

## Audit findings deferred (not fixed this patch)

| # | File | Issue | Reason |
|---|------|-------|--------|
| 7 | CoveragePlannerView | `hasFilled` semantics when both broadcast+assignment exist | Design decision — broadcast state takes visual priority |
| 8 | CoveragePlannerView | Potential stale `supplyFor` in `activeAssignedForDrawer` deps | Needs careful regression testing; low frequency |
| 9 | OpenShiftManager | `t` prop threading through `CompactFormFields` | Refactored type; full extraction is cosmetic |
| 10 | CoveragePlannerView | Misleading `isoDow` function name | Cosmetic; no logic impact |

## Tests
TypeScript: `npx tsc --noEmit` — 0 errors.
