# v3.30.0 — Plugin Marketplace (Top-20 Rank 19)

**Date:** 2026-05-14
**Status before:** MISSING (green-field).
**Status after:** DONE for the marketplace MVP. Sandboxed runtime + plugin SDK npm package deferred.

## What ships

- `marketplace_plugins` table (system-wide catalog with approval workflow).
- `workspace_installed_plugins` (per-workspace install + config).
- `plugin_webhook_events` (event-dispatch log).
- 4 SECURITY DEFINER RPCs (submit / approve / install / uninstall).
- 2 seeded sample plugins (slack-leave-notify, birthday-bot).
- `PluginMarketplacePanel` browse + install/uninstall UI inside Settings tab.
- Enterprise-tier only.
- 90 localized strings × 5 locales.

## Deferred (v3.30.1+)

- Sandboxed plugin runtime with scoped API keys.
- `@effectime/plugin-sdk` npm package.
- Webhook dispatcher edge function with HMAC-SHA256 signing + exponential retry.
